document.querySelector('.hamburger-menu').addEventListener('click',() => [
    document.querySelector('.container-fluid').classList.toggle("change")
]);

setTimeout(() => [
    documet.querySelector("html").style.scrollBehavior = "unset"
], 1000

);
// document.querySelector('.menu-item').addEventListener('click',() =>).toggle;